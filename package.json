{
  "name": "monorepo-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "web": "cd web && npm run dev",
    "server": "cd server &&  npm run start",
    "dev": "concurrently \"npm run web\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}